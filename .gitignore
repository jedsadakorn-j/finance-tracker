# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# cloudflare / wrangler local state
.wrangler/
# NOTE: worker-configuration.d.ts is committed so CI can typecheck the Worker.
# It contains only binding TYPE names (e.g. ADMIN_PASSWORD: string), never values.
# Regenerate after changing bindings with: npm run cf-typegen

# secrets — NEVER commit these
.dev.vars
.env
.env.*
!.env.example

# misc
.DS_Store
*.log
.vite/
